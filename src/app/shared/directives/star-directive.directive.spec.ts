import { StarDirectiveDirective } from './star-directive.directive';

describe('StarDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StarDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
